# Список разрешённого, а не запрещённого: в образ случайно не уедет ни папка
# с купленным паком (512 МБ рядом с кодом), ни чужие аквариумы из data.
*

!server.js
!package.json
!*.html
!assets
!vendor
!demos
!tools

# Пак монтируется томом на сервере, в образе ему делать нечего.
assets/models/pack

# Инструменты сборки листов на сервере не нужны, но весят копейки и
# помогают отладке — оставлены сознательно.
